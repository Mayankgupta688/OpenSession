export default function WorkingWithProps(props) {
    debugger;
    props.userDetails.userName = "gkgujku";
    return <h1>Hello {props.userName} Aged {props.userAge}.... {props.userDetails.userName}</h1>
} 